# macOS
.DS_Store

# Xcode / Swift
build/
DerivedData/
*.xcuserstate
xcuserdata/
.swiftpm/
.build/

# Local config / secrets
*.local

# Vendored whisper-cli + model (fetched at build time, never committed)
vendor/
