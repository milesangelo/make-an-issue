---
phase: 03-local-transcription
plan: 05
type: execute
wave: 1
depends_on: []
files_modified:
  - scripts/fetch-whisper.sh
  - scripts/build-app.sh
autonomous: true
gap_closure: true
requirements: [TRANSCRIBE-01, TRANSCRIBE-02]

must_haves:
  truths:
    - "The assembled MakeAnIssue.app runs the bundled whisper-cli from dylibs co-located inside the app, with no dependency on the whisper.cpp build tree."
    - "The bundled whisper-cli's only rpath is @loader_path — no absolute build-tree path remains."
    - "A clean re-run of scripts/fetch-whisper.sh verifies the model SHA256 against the pinned digest and exits 0."
  artifacts:
    - path: "scripts/fetch-whisper.sh"
      provides: "Pinned MODEL_SHA256 + vendoring of the six @rpath dylibs into vendor/"
      contains: "c6138d6d58ecc8322097e0f987c32f1be8bb0a18532a3f88f734d1bbf9c41e5d"
    - path: "scripts/build-app.sh"
      provides: "Bundles dylibs into Resources, rewrites whisper-cli LC_RPATH to @loader_path, ad-hoc signs dylibs then whisper-cli (bottom-up)"
      contains: "install_name_tool"
  key_links:
    - from: "scripts/fetch-whisper.sh"
      to: "vendor/whisper.cpp-src/build/bin/lib*.dylib"
      via: "cp dereferences the build-tree symlinks into flat vendor/lib*.dylib real files"
      pattern: "build/bin/.*dylib"
    - from: "scripts/build-app.sh"
      to: "Contents/Resources/whisper-cli LC_RPATH"
      via: "install_name_tool -delete_rpath <abs build path> + -add_rpath @loader_path"
      pattern: "install_name_tool.*loader_path"
    - from: "scripts/build-app.sh"
      to: "Contents/Resources/lib*.dylib"
      via: "cp vendored dylibs into Resources and codesign each ad-hoc before signing whisper-cli"
      pattern: "codesign.*lib"
---

<objective>
Close the two diagnosed UAT gaps from 03-VERIFICATION.md so the assembled `.app` is genuinely self-contained and a clean checkout can verify the model.

GAP 1 (major): `scripts/build-app.sh` bundles only `whisper-cli` + the model. It never bundles the six `@rpath` dylibs whisper-cli links, never rewrites whisper-cli's absolute `LC_RPATH` (`$repo_root/vendor/whisper.cpp-src/build/bin`) to a loader-relative path, and never signs the dylibs. The app runs today only because that build tree still sits on disk. `scripts/fetch-whisper.sh` vendors only the binary, leaving build-app with no stable dylib source.

GAP 2 (minor): `scripts/fetch-whisper.sh` line 9 still carries the unpinned placeholder for `MODEL_SHA256`. The verify branch is correct; only the pinned digest is missing. The real content digest is already computed: `c6138d6d58ecc8322097e0f987c32f1be8bb0a18532a3f88f734d1bbf9c41e5d`.

Purpose: A copy of `Contents/Resources/` (whisper-cli + model + dylibs) must transcribe with zero reliance on the whisper.cpp build tree, and a clean re-run of fetch-whisper must reach and pass the checksum verify.
Output: Edited `scripts/fetch-whisper.sh` and `scripts/build-app.sh`. No app-code (Swift) changes.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-local-transcription/03-UAT.md
@scripts/fetch-whisper.sh
@scripts/build-app.sh

# Verified facts (gathered during planning — treat as ground truth, do NOT re-derive):
# - `otool -L vendor/whisper-cli` links exactly six @rpath dylibs:
#     @rpath/libwhisper.1.dylib, @rpath/libggml.0.dylib, @rpath/libggml-base.0.dylib,
#     @rpath/libggml-cpu.0.dylib, @rpath/libggml-blas.0.dylib, @rpath/libggml-metal.0.dylib
#   plus /usr/lib/libc++.1.dylib and /usr/lib/libSystem.B.dylib (system; never bundled).
# - whisper-cli's single LC_RPATH is the absolute build path
#     $repo_root/vendor/whisper.cpp-src/build/bin
# - In vendor/whisper.cpp-src/build/bin/, the six names above are SYMLINKS to versioned
#   real files (e.g. libwhisper.1.dylib -> libwhisper.1.9.1.dylib). `cp` dereferences a
#   symlink source by default, producing a flat real file named exactly as the @rpath
#   basename.
# - vendor/whisper.cpp-src/samples/jfk.wav exists (separate from build/bin) — valid input.
# - `./vendor/whisper-cli -m vendor/ggml-small.en.bin -f <jfk.wav> -l en -nt -t 4`
#   prints the transcript (contains "country") to STDOUT and exits 0.
# - vendor/ is gitignored; these dylibs/model never enter git history.
</context>

<tasks>

<task type="auto">
  <name>Task 1: fetch-whisper.sh — pin MODEL_SHA256 (GAP 2) and vendor the six @rpath dylibs (GAP 1a)</name>
  <files>scripts/fetch-whisper.sh</files>
  <action>
    Two surgical edits to scripts/fetch-whisper.sh, matching its existing POSIX `sh` / `set -eu` style.

    EDIT A (GAP 2 — pin the model digest, line 9 region):
    - Replace the placeholder value of `MODEL_SHA256` with the real, already-computed content digest `c6138d6d58ecc8322097e0f987c32f1be8bb0a18532a3f88f734d1bbf9c41e5d`.
    - Update the now-stale preceding comment so it no longer claims the value is unpinned (it currently begins "UNPINNED: Replace with..."). Re-word it to state the value is the pinned 64-char content SHA256 of ggml-small.en.bin. KEEP the existing sentence warning not to use the 40-char HuggingFace LFS pointer hash.
    - Do NOT touch the gating comparison further down the file (the first-download `if [ "$MODEL_SHA256" = ... ]` branch and its sentinel must remain intact — pinning the value simply makes that branch fall through to the existing `shasum -a 256 -c -` verify).

    EDIT B (GAP 1a — vendor the dylibs whisper-cli loads):
    - After the existing whisper-cli build block (the `if [ ! -f "$VENDOR/whisper-cli" ]` guard ending with `echo "whisper-cli built at $WHISPER_TAG"`) and before the model-download block, add a new guarded block that copies the six dylibs from `$SRC/build/bin/` into `$VENDOR/`.
    - Define the list once: the six basenames are `libwhisper.1.dylib libggml.0.dylib libggml-base.0.dylib libggml-cpu.0.dylib libggml-blas.0.dylib libggml-metal.0.dylib` — the exact `@rpath` basenames whisper-cli links. Use a shell variable and a `for` loop (POSIX sh).
    - Guard: only copy when at least one dylib is missing from `$VENDOR/` (idempotent — skip if all six already vendored). This MUST run independently of the whisper-cli build guard, because that guard skips a rebuild when the binary already exists, which would otherwise leave the dylibs un-vendored.
    - Source resolution: `cp "$SRC/build/bin/$lib" "$VENDOR/$lib"`. The source `$lib` is a symlink in build/bin; `cp` dereferences it, so `$VENDOR/$lib` becomes a flat REAL Mach-O file named exactly as the `@rpath` basename (no symlink chains at the destination). Run `xattr -cr "$VENDOR/$lib"` on each, matching the existing whisper-cli handling.
    - If a copy is needed but `$SRC/build/bin` does not exist (binary vendored but source tree removed), fail fast with a clear message to `rm -rf "$VENDOR/whisper-cli" "$SRC"` and re-run — do not silently continue.
    - Print a one-line confirmation listing the vendored dylibs.

    Reference: UAT GAP 1 root cause — fetch-whisper.sh copied only build/bin/whisper-cli, leaving build-app with no stable dylib source. This edit supplies that source.
  </action>
  <verify>
    <automated>cd "$(git rev-parse --show-toplevel)" && grep -Eq '^MODEL_SHA256="c6138d6d58ecc8322097e0f987c32f1be8bb0a18532a3f88f734d1bbf9c41e5d"$' scripts/fetch-whisper.sh && sh -n scripts/fetch-whisper.sh && scripts/fetch-whisper.sh && for lib in libwhisper.1.dylib libggml.0.dylib libggml-base.0.dylib libggml-cpu.0.dylib libggml-blas.0.dylib libggml-metal.0.dylib; do test -f "vendor/$lib" || { echo "MISSING vendor/$lib"; exit 1; }; file "vendor/$lib" | grep -q 'Mach-O' || { echo "vendor/$lib not a real dylib"; exit 1; }; done && echo "TASK1 OK"</automated>
  </verify>
  <done>
    - `MODEL_SHA256` line equals the real 64-char digest; `sh -n` parses clean.
    - A re-run of `scripts/fetch-whisper.sh` reaches the `shasum -a 256 -c -` verify and exits 0 (prints "ggml-small.en.bin downloaded and verified"), closing GAP 2.
    - All six `vendor/lib*.dylib` exist as real Mach-O files (not symlinks), giving build-app a stable source — closing the fetch-whisper half of GAP 1.
  </done>
</task>

<task type="auto">
  <name>Task 2: build-app.sh — bundle dylibs, rewrite LC_RPATH to @loader_path, ad-hoc sign bottom-up (GAP 1b)</name>
  <files>scripts/build-app.sh</files>
  <action>
    Edit scripts/build-app.sh to make the assembled bundle self-contained. Match the existing POSIX `sh` / `set -eu` style and the `$repo_root` / `$resources_dir` variables already in scope.

    1. Extend the existing vendor preflight guard (currently checks `vendor/whisper-cli` and `vendor/ggml-small.en.bin`) to ALSO require the six dylibs in `vendor/`. Define the list once as a shell variable: `libwhisper.1.dylib libggml.0.dylib libggml-base.0.dylib libggml-cpu.0.dylib libggml-blas.0.dylib libggml-metal.0.dylib`. If any is missing, exit 1 with the existing-style message instructing the developer to run `scripts/fetch-whisper.sh` first.

    2. After the existing `cp` of whisper-cli + model into `$resources_dir` (and the `chmod +x`), copy each vendored dylib into `$resources_dir/` with a `for` loop over the same list. `@loader_path` resolves to the directory containing whisper-cli, so co-locating the dylibs in Resources is what makes the binary find them.

    3. Rewrite whisper-cli's rpath BEFORE signing it (install_name_tool invalidates any signature, so this must precede the codesign of whisper-cli). Derive the current absolute build rpath from the binary itself rather than hardcoding a home path: read the single `LC_RPATH` path from `otool -l "$resources_dir/whisper-cli"` (the path line two lines after the `cmd LC_RPATH` marker). If that value is non-empty and not already `@loader_path`, run `install_name_tool -delete_rpath "<that path>" "$resources_dir/whisper-cli"`. Then unconditionally run `install_name_tool -add_rpath "@loader_path" "$resources_dir/whisper-cli"`. (The bundle is rebuilt fresh each run via the existing `rm -rf "$app_dir"`, and whisper-cli is re-copied from vendor with only the absolute rpath, so add_rpath runs exactly once — no duplicate.)

    4. Sign bottom-up: ad-hoc `codesign --force -s -` EACH bundled dylib (loop over the list) FIRST, then keep the existing ad-hoc sign of whisper-cli LAST. The existing whisper-cli codesign line stays at the end so it runs after the install_name_tool edits.

    Do NOT modify the dylibs' own rpaths or install names: `@rpath` references between dylibs resolve against the main executable's (whisper-cli's) `@loader_path`, so rewriting whisper-cli's rpath alone is sufficient and is what the diagnosis specifies. Full Developer-ID / hardened-runtime / notarization remains DEFERRED (D-04/D-05) — keep ad-hoc signing.

    Reference: UAT GAP 1 missing items — bundle the dylibs, rewrite the absolute LC_RPATH to @loader_path, and sign the dylibs bottom-up.
  </action>
  <verify>
    <automated>cd "$(git rev-parse --show-toplevel)" && sh -n scripts/build-app.sh && scripts/build-app.sh && R=".build/MakeAnIssue.app/Contents/Resources" && for lib in libwhisper.1.dylib libggml.0.dylib libggml-base.0.dylib libggml-cpu.0.dylib libggml-blas.0.dylib libggml-metal.0.dylib; do test -f "$R/$lib" || { echo "MISSING $R/$lib"; exit 1; }; codesign -v "$R/$lib" || { echo "UNSIGNED $R/$lib"; exit 1; }; done && otool -l "$R/whisper-cli" | grep -A2 LC_RPATH | grep -q '@loader_path' || { echo "no @loader_path rpath"; exit 1; } && ! otool -l "$R/whisper-cli" | grep -A2 LC_RPATH | grep -q 'build/bin' || { echo "absolute build rpath still present"; exit 1; } && otool -L "$R/whisper-cli" | grep -vE 'whisper-cli:|@rpath/|@loader_path/|/usr/lib/' | grep -q '.' && { echo "unexpected non-@rpath/system link"; exit 1; } || true && ST="$(mktemp -d)" && cp "$R/whisper-cli" "$R/ggml-small.en.bin" "$R"/lib*.dylib "$ST/" && "$ST/whisper-cli" -m "$ST/ggml-small.en.bin" -f vendor/whisper.cpp-src/samples/jfk.wav -l en -nt -t 4 | grep -qi 'country' && echo "TASK2 OK SELF-CONTAINED" && rm -rf "$ST"</automated>
  </verify>
  <done>
    - All six dylibs are present in `Contents/Resources/` and each passes `codesign -v`.
    - `otool -l` on the bundled whisper-cli shows `@loader_path` and NO absolute `build/bin` rpath; `otool -L` shows only `@rpath`/`@loader_path` + `/usr/lib` links.
    - Running whisper-cli from a temp dir holding ONLY the bundled artifacts (no build tree reachable via @loader_path) transcribes jfk.wav (output contains "country") and exits 0 — proving the bundle is self-contained. Closes GAP 1.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| network → vendor/ggml-small.en.bin | Model downloaded from HuggingFace over the network (untrusted bytes until verified) |
| local build → bundled binary | whisper-cli + dylibs built from pinned source, then executed inside the .app |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-01 | Tampering | model download (curl from HuggingFace) | mitigate | Pin MODEL_SHA256 to the 64-char content digest; `shasum -a 256 -c -` aborts on mismatch (Task 1, GAP 2 fix) |
| T-03-02 | Tampering | whisper.cpp build source | mitigate | Cloned at pinned tag v1.9.1 and built from source (no prebuilt binary fetched); rpath rewritten to @loader_path so the binary loads only bundled, ad-hoc-signed dylibs (Task 2) |
| T-03-03 | Elevation | bundled whisper-cli execution | accept | Ad-hoc signed; runs locally only. Developer-ID + hardened-runtime notarization for clean-machine distribution is DEFERRED to a future release phase (D-04/D-05) |
| T-03-SC | Tampering | npm/pip/cargo installs | accept | N/A — this plan performs no package-manager installs; all native artifacts are built from the pinned whisper.cpp source. No legitimacy audit required. |
</threat_model>

<verification>
End-to-end gap-closure verification (run from repo root):

1. GAP 2 — clean re-run: `scripts/fetch-whisper.sh` reaches the `shasum -a 256 -c -` line and exits 0 ("ggml-small.en.bin downloaded and verified"). The `MODEL_SHA256` assignment equals `c6138d6d58ecc8322097e0f987c32f1be8bb0a18532a3f88f734d1bbf9c41e5d`.
2. GAP 1 — vendor source: all six `vendor/lib*.dylib` exist as real Mach-O files.
3. GAP 1 — bundle: after `scripts/build-app.sh`, `Contents/Resources/` contains the six dylibs (each `codesign -v` clean) plus whisper-cli (signed last) and the model.
4. GAP 1 — rpath: `otool -l Contents/Resources/whisper-cli` shows `@loader_path` and no absolute `build/bin` rpath; `otool -L` shows only `@rpath`/`@loader_path` + `/usr/lib`.
5. GAP 1 — self-containment proof: copying ONLY the bundled artifacts (whisper-cli + model + dylibs) to a temp dir and running whisper-cli there transcribes jfk.wav (output contains "country") and exits 0, with no whisper.cpp build tree reachable via @loader_path.

Note: the full push-to-talk GUI transcription path was already verified PASS in 03-UAT Test 1; this plan's gap is specifically self-containment, which the temp-dir subprocess proof establishes deterministically (dyld either resolves all dylibs or fails to launch).
</verification>

<success_criteria>
- A clean re-run of `scripts/fetch-whisper.sh` verifies the model checksum and exits 0 (GAP 2 closed).
- The assembled `MakeAnIssue.app` bundles all six `@rpath` dylibs in `Contents/Resources/`, each ad-hoc signed, with whisper-cli's rpath rewritten to `@loader_path` and no absolute build-tree path remaining (GAP 1 closed).
- whisper-cli, run in isolation from only the bundled artifacts, transcribes a sample WAV successfully — proving the bundle no longer depends on `vendor/whisper.cpp-src/build/`.
- Only `scripts/fetch-whisper.sh` and `scripts/build-app.sh` changed; no Swift/app-code edits.
</success_criteria>

<output>
Create `.planning/phases/03-local-transcription/03-05-SUMMARY.md` when done.
</output>
