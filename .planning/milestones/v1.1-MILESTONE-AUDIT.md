---
milestone: v1.1
name: Concurrent Filing & Control
audited: 2026-07-02
revalidated: 2026-07-17
status: passed
scores:
  requirements: 15/15
  phases: 5/5
  integration: 12/12
  flows: 5/5
gaps:
  requirements: []
  integration: []
  flows: []
tech_debt:
  - phase: 06-cancellation-stop-control
    items:
      - "WR-01: onCancel SIGKILL escalation (CLIRunner.swift ~219) is unconditional — no process.isRunning guard; latent footgun under PID reuse within the 2s window. Benign in practice (ESRCH)."
      - "WR-02: pre-launch cancel (CLIRunner.swift ~184) sends SIGTERM with no bounded SIGKILL escalation — a SIGTERM-ignoring process survives to the 300s timeout."
      - "WR-06: .terminateLater quit path (forceKillAllProcessTrees) has no automated test — behaviour-critical slow path verified only by manual/UAT gate."
      - "OBS-01: group-kill signals -process.processIdentifier assuming the spawned zsh is its own process-group leader; relies on Foundation Process default spawn behavior rather than explicit setpgid. Within Phase 6 scope, already verified; noted by integration checker."
  - phase: 08-editable-system-prompt-finding-06-cleanup
    items:
      - "WR-02: enforcedTrailer doc comment (IssueFilingRunner.swift) frames the soft prompt-level trailer as equivalent to the hard --allowedTools boundary — documentation precision only, no code defect."
nyquist:
  compliant_phases: []
  partial_phases: [05, 06, 07, 08, 09]
  missing_phases: []
  overall: partial
---

# Milestone v1.1 — Concurrent Filing & Control — Audit Report

**Audited:** 2026-07-02
**Status:** ✓ passed
**Definition of done (ROADMAP):** Phases 5–9 — remove the serial-filing bottleneck and give the user control over filing and the LLM prompt.

## Summary

**The milestone is complete.** All five phases (5–9) are executed, individually verified, and integrate cleanly end-to-end. All 15 milestone requirements are satisfied under a 3-source cross-reference (VERIFICATION.md + SUMMARY frontmatter + REQUIREMENTS.md traceability). Cross-phase integration is 12/12 WIRED with zero orphans and zero broken flows.

This supersedes the 2026-07-01 audit, which reported `gaps_found` because Phase 9 (JOBS-01, JOBS-02, RESIL-01) had not yet been started. Phase 9 shipped on 2026-07-02 (both plans executed, `09-VERIFICATION.md` passed 11/11), closing all three previously-orphaned requirements and wiring `AppState.cancel(jobID:)` — the sole export that previously lacked a non-test call site — to the per-row Stop button.

## Requirements Coverage (3-source cross-reference)

| REQ-ID | Phase | VERIFICATION | SUMMARY frontmatter | REQUIREMENTS.md | Final |
|--------|-------|--------------|---------------------|-----------------|-------|
| CONCUR-01 | 5 | passed | provides CONCUR-01 tests | `[x]` | **satisfied** |
| CONCUR-02 | 5 | passed | provides CONCUR-02 tests | `[x]` | **satisfied** |
| CONCUR-03 | 5 | passed | provides CONCUR-03 tests | `[x]` | **satisfied** |
| CANCEL-01 | 6 | passed | requirements: CANCEL-01 | `[x]` | **satisfied** |
| CANCEL-02 | 6 | passed | requirements: CANCEL-02 | `[x]` | **satisfied** |
| CANCEL-03 | 6 | passed | requirements: CANCEL-03 | `[x]` | **satisfied** |
| SETTINGS-01 | 7 | passed | (07 exports) | `[x]` | **satisfied** |
| SETTINGS-02 | 8 | passed | requirements-completed | `[x]` | **satisfied** |
| SETTINGS-03 | 8 | passed | requirements-completed | `[x]` | **satisfied** |
| SETTINGS-04 | 8 | passed | requirements-completed | `[x]` | **satisfied** |
| SETTINGS-05 | 8 | passed | requirements-completed | `[x]` | **satisfied** |
| FEEDBACK-02 | 7 | passed | (07 exports) | `[x]` | **satisfied** |
| JOBS-01 | 9 | passed | requirements-completed | `[x]` | **satisfied** |
| JOBS-02 | 9 | passed | requirements-completed | `[x]` | **satisfied** |
| RESIL-01 | 9 | passed | requirements-completed | `[x]` | **satisfied** |

**Score: 15/15 satisfied.** Phases 5–7 SUMMARYs use a `requirements:`/`provides:` frontmatter key rather than `requirements-completed:`; all such entries reconcile against a `passed`/`verified` VERIFICATION.md and a `[x]` traceability row, so they resolve to satisfied.

## Phase Verification Roll-up

| Phase | Status | Score | Notes |
|-------|--------|-------|-------|
| 05 Concurrent Filing Jobs Model | passed | 7/7 | No anti-patterns. |
| 06 Cancellation / Stop Control | passed | 4/4 | UAT-closed 2026-06-30; 3 WARNING-level robustness items (tech debt). |
| 07 AppKit Status-Item UI + Settings Shell | passed | 7/7 | UAT-closed 2026-07-01 (6/6 human checks). |
| 08 Editable System Prompt + FINDING-06 Cleanup | passed | 14/14 | WR-01 (stale message) fixed (commit 99db72c); WR-02 doc-only. |
| 09 Jobs List UI + Per-Job Stop + Surfaced Errors | passed | 11/11 | UAT-approved; JobsSection/JobRow verified via human-verify. |

## Cross-Phase Integration

Integration checker verified **12/12** cross-phase connections as WIRED, **0** orphaned, **0** broken. All **5/5** E2E flows trace fully:

1. Dictate → file concurrently → spoken confirmation — WIRED (`AppState.swift:246→248→spawnFilingJob→:301`)
2. Dictate → per-row Stop mid-filing → cancelled — WIRED (`MenuView.swift:560` Stop → `cancel(jobID:)` → `CLIRunner` group-kill → `.cancelled` + announce) — **the JOBS-02 UI surface for CANCEL-01, previously missing, is now present**
3. Edit instructions → filing uses them with enforced trailer — WIRED (`SettingsView.swift:7` → `instructionsKey` → `AppState.swift:122` → `buildPrompt` enforcedTrailer-last `:95`)
4. Quit mid-filing → clean teardown — WIRED (`AppDelegate.swift:34` `cancelAll` → sweep → `:42` `forceKillAllProcessTrees` → `:43` backstop sweep)
5. Failed filing → persistent recoverable error row — WIRED (`AppState.swift:311` `.failed` → `MenuView.swift:588` row with message + `TranscriptSnippet` + dismiss)

The prior audit's single orphan — `AppState.cancel(jobID:)` with no non-test call site — is now resolved: the JobRow `.filing` case `Button("Stop")` at `MenuView.swift:560` calls it directly.

## Nyquist Coverage

| Phase | VALIDATION.md | nyquist_compliant | wave_0_complete | Classification |
|-------|---------------|-------------------|-----------------|----------------|
| 05 | exists | false | false | PARTIAL |
| 06 | exists | false | false | PARTIAL |
| 07 | exists | false | false | PARTIAL |
| 08 | exists | false | true (validated) | PARTIAL |
| 09 | exists | false | false | PARTIAL |

**Overall: partial.** VALIDATION.md exists for every phase but none reaches `nyquist_compliant: true`. This is a process-coverage gap, not a product gap — it does **not** block milestone completion. Optionally run `/gsd-validate-phase 5 6 7 8 9` to close Nyquist gaps retroactively.

## Tech Debt (non-blocking)

**Phase 06** — robustness warnings from 06-VERIFICATION.md + one integration-checker observation:
- WR-01: unconditional `onCancel` SIGKILL escalation (no `isRunning` guard) — latent PID-reuse footgun, benign in practice.
- WR-02: pre-launch cancel path has no bounded SIGKILL escalation.
- WR-06: `.terminateLater` `forceKillAllProcessTrees()` slow path has no automated test.
- OBS-01: group-kill assumes spawned zsh is its own process-group leader via Foundation `Process` default spawn (no explicit `setpgid`). Within Phase 6 scope, already verified.

**Phase 08** — 1 doc-only warning:
- WR-02: `enforcedTrailer` doc comment overstates a soft prompt-level guarantee as equivalent to the hard tool-scope boundary. (WR-01 stale error message already fixed in commit 99db72c.)

**Total: 5 items across 2 phases.** None blocks any satisfied requirement.

## Verdict

The milestone as defined (Phases 5–9) is **complete and audit-clean**: 15/15 requirements satisfied, 5/5 phases verified, 12/12 integration connections wired, 5/5 E2E flows traced. Remaining items are non-blocking: 5 tech-debt entries (Phase 6 robustness ×4, Phase 8 doc-only ×1) and partial Nyquist coverage across all five phases. v1.1 is ready to complete.
