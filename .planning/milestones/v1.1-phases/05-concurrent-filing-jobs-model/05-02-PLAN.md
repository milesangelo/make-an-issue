---
phase: 05-concurrent-filing-jobs-model
plan: 02
type: execute
wave: 2
depends_on:
  - 05-01
files_modified:
  - Tests/MakeAnIssueTests/AppStateTests.swift
autonomous: true
requirements:
  - CONCUR-01
  - CONCUR-02
  - CONCUR-03

must_haves:
  truths:
    - "A test proves captureState == .idle AND jobs.count == 1 the moment transcription completes, while the filing stub is still in-flight (SC-1 / CONCUR-01)"
    - "A test proves a new recording can start while jobs[0].state == .filing, and that two filing jobs can be in flight at once (SC-2 / CONCUR-02 / D-09)"
    - "Tests prove each completing job speaks its own outcome and that announcements are deferred during .recording and flushed on stop (SC-3 / CONCUR-03 / D-01 / D-02 / D-03 / D-04)"
    - "Tests prove terminal jobs are retained in jobs after completion (D-06 / D-07)"
    - "SC-4 is verified-not-rebuilt: IssueFilingRunner.file remains a static func with per-invocation UUID tempfile + defer cleanup, and two concurrent stub filings complete without interference"
  artifacts:
    - path: "Tests/MakeAnIssueTests/AppStateTests.swift"
      provides: "the new concurrency, announcement, retention, and SC-4 isolation tests"
      contains: "testTwoConcurrentFilingJobsCanBeSpawned"
  key_links:
    - from: "Tests/MakeAnIssueTests/AppStateTests.swift"
      to: "Sources/MakeAnIssue/AppState.swift (spawnFilingJob / announce / jobs)"
      via: "seam injection — onRunIssueFiling stubs hold jobs in-flight, onSpeak captures announcements"
      pattern: "onRunIssueFiling"
    - from: "Tests/MakeAnIssueTests/AppStateTests.swift (SC-4 source check)"
      to: "Sources/MakeAnIssue/IssueFilingRunner.swift (~lines 141-147)"
      via: "verify-not-rebuild — static func + UUID tempfile + defer cleanup unchanged"
      pattern: "make-an-issue-mcp-"
---

<objective>
Prove the Phase 5 jobs model satisfies CONCUR-01/02/03 with a comprehensive XCTest suite driven
through AppState's existing injection seams (`onRunIssueFiling`, `onSpeak`, `onRunTranscription`).
These are additive tests on top of the green refactor delivered by Plan 01 — they characterize and
lock the concurrency, announcement-deferral, retention, and tempfile-isolation behaviors.

Purpose: Lock CONCUR-01 (idle immediately), CONCUR-02 (concurrent + distinct transcripts),
CONCUR-03 (independent spoken outcomes + defer/flush), plus D-06/D-07 retention and SC-4 isolation,
as regression-protected automated assertions.
Output: New test methods in `AppStateTests.swift`; full suite green; an SC-4 source-verification check.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-concurrent-filing-jobs-model/05-CONTEXT.md
@.planning/phases/05-concurrent-filing-jobs-model/05-RESEARCH.md
@.planning/phases/05-concurrent-filing-jobs-model/05-PATTERNS.md
@.planning/phases/05-concurrent-filing-jobs-model/05-01-SUMMARY.md
</context>

## Artifacts this phase produces

This plan adds tests only — it produces no new production symbols. It exercises the symbols
produced by Plan 01: `FilingJob` / `FilingJobState`, `AppState.jobs`, `spawnFilingJob`, `announce`,
`flushPendingAnnouncements`, and the 3-case `CaptureState`. See 05-01-PLAN.md "Artifacts this phase
produces" for the full symbol inventory. The SC-4 step verifies (does not modify)
`IssueFilingRunner.file`'s per-invocation UUID tempfile + `defer` cleanup.

<tasks>

<task type="auto">
  <name>Task 1: CONCUR-01 + CONCUR-02 + D-09 capture/concurrency tests</name>
  <files>Tests/MakeAnIssueTests/AppStateTests.swift</files>
  <read_first>
    - Tests/MakeAnIssueTests/AppStateTests.swift (seam-injection pattern 426-452; `waitUntil` 682-693; `makeRepo` 695-699; the Plan 01 rewrites)
    - Sources/MakeAnIssue/AppState.swift (refactored — `jobs`, `spawnFilingJob`, `startRecording` guard, `beginTranscription` success path)
    - Sources/MakeAnIssue/FilingJob.swift (`FilingJob` / `FilingJobState` for `jobs[i].state` / `jobs[i].transcript` assertions)
    - .planning/phases/05-concurrent-filing-jobs-model/05-RESEARCH.md (Validation Architecture — New Tests Required table; in-flight + concurrent-spawn patterns)
    - .planning/phases/05-concurrent-filing-jobs-model/05-PATTERNS.md (new-test patterns: in-flight job observation, concurrent job spawning)
  </read_first>
  <action>
    Add four tests using a bound repo (via `makeRepo`) and a slow `onRunIssueFiling` stub
    (`try? await Task.sleep(for: .milliseconds(300))` before returning an `IssueFilingResult`) so jobs
    stay in-flight long enough to observe. `testTranscriptionCompletionReturnsCaptureToIdleImmediately`:
    after one record/stop cycle, `await waitUntil { state.jobs.count == 1 }` then assert
    `state.captureState == .idle` while `state.jobs[0].state == .filing` (CONCUR-01 / SC-1 — capture is
    idle before filing finishes). `testNewRecordingAllowedWhileFilingIsInFlight`: while
    `state.jobs[0].state == .filing`, call `state.startRecording()` and assert `state.captureState ==
    .recording` (D-09 — re-entry allowed). `testPTTReEntryDuringFilingStartsNewRecording`: same intent
    asserting the guard now admits PTT during in-flight filings (D-09). `testTwoConcurrentFilingJobsCanBeSpawned`:
    run two full record/stop cycles, `await waitUntil { state.jobs.count == 1 }` after the first and
    `await waitUntil { state.jobs.count == 2 }` after the second while both stubs are still sleeping, then
    assert both `state.jobs[0].state` and `state.jobs[1].state` equal `.filing` simultaneously
    (CONCUR-02 / SC-2). Use only the existing `waitUntil`/`makeRepo` helpers and the established seam
    structure; assert real behavior, never weaken to pass.
  </action>
  <verify>
    <automated>swift test --filter AppStateTests 2>&1 | tail -8</automated>
  </verify>
  <acceptance_criteria>
    - All four new tests pass under `swift test --filter AppStateTests`.
    - A test asserts `captureState == .idle` while `jobs[0].state == .filing` (CONCUR-01 source/behavior assertion).
    - A test asserts `jobs.count == 2` with both jobs `.filing` at the same time (CONCUR-02 behavior assertion).
    - A test asserts a second `startRecording()` reaches `.recording` while a job is `.filing` (D-09 behavior assertion).
  </acceptance_criteria>
  <done>CONCUR-01, CONCUR-02, and D-09 capture/concurrency behaviors are locked by passing tests.</done>
</task>

<task type="auto">
  <name>Task 2: CONCUR-02 distinct-transcript + CONCUR-03 announcement tests</name>
  <files>Tests/MakeAnIssueTests/AppStateTests.swift</files>
  <read_first>
    - Tests/MakeAnIssueTests/AppStateTests.swift (Task 1 additions; `onSpeak` capture pattern in `testSuccessfulFilingSpeaksIssueNumber` 426-452)
    - Sources/MakeAnIssue/AppState.swift (`announce`, `flushPendingAnnouncements`, `speakText`, `spawnFilingJob` success/failure branches)
    - .planning/phases/05-concurrent-filing-jobs-model/05-CONTEXT.md (D-01 success text, D-02/D-03 defer/flush, D-04/D-05 generic failure text)
    - .planning/phases/05-concurrent-filing-jobs-model/05-RESEARCH.md (CONCUR-03 tests; defer/flush patterns)
  </read_first>
  <action>
    Add five tests capturing spoken text via an `onSpeak: { text in spokenTexts.append(text) }` array
    closure. `testBothConcurrentJobsRetainDistinctTranscripts`: two cycles whose `onRunTranscription`
    stubs return distinct strings ("first transcript", "second transcript"); after `jobs.count == 2`
    assert `state.jobs[0].transcript` and `state.jobs[1].transcript` differ and match the two inputs
    (CONCUR-02 / Pitfall 1 — no stale-transcript bleed). `testSuccessfulFilingJobSpeaksIssueNumber`:
    a successful filing stub (number 42) → `await waitUntil { !spokenTexts.isEmpty }`, assert a spoken
    string contains "42" and matches the `created issue #N` shape (D-01 / CONCUR-03).
    `testFailedFilingJobSpeaksGenericFailure`: an `onRunIssueFiling` stub that throws
    `IssueFilingError.timeout` → assert the spoken string equals "issue filing failed" (D-04 / D-05 —
    one generic phrase, no per-type reason). `testAnnouncementDeferredDuringRecording`: drive the app so
    a job completes while `captureState == .recording` (start a new recording before the prior job's
    stub returns) and assert `onSpeak` is NOT called yet while recording — the announcement sits in the
    pending buffer (D-02 / D-03). `testDeferredAnnouncementFlushedOnRecordingStop`: from that deferred
    state, call `state.stopRecording()` (which enters `beginTranscription` and flushes) and
    `await waitUntil { !spokenTexts.isEmpty }`, asserting the deferred announcement is then spoken
    (D-03). Use existing helpers; assert real behavior.
  </action>
  <verify>
    <automated>swift test --filter AppStateTests 2>&1 | tail -8</automated>
  </verify>
  <acceptance_criteria>
    - All five new tests pass under `swift test --filter AppStateTests`.
    - A test asserts the two concurrent jobs carry distinct, correct transcripts (CONCUR-02 / Pitfall 1).
    - A test asserts a success announcement contains the issue number (D-01) and another asserts the failure announcement equals "issue filing failed" (D-04/D-05).
    - A test asserts `onSpeak` is not called while `captureState == .recording` (D-02) and a paired test asserts the deferred text is spoken after `stopRecording()` (D-03).
  </acceptance_criteria>
  <done>CONCUR-03 announcement behavior — success text, generic failure text, defer-during-recording, flush-on-stop — and distinct-transcript isolation are locked by passing tests.</done>
</task>

<task type="auto">
  <name>Task 3: D-06/D-07 retention tests + SC-4 tempfile-isolation verification</name>
  <files>Tests/MakeAnIssueTests/AppStateTests.swift</files>
  <read_first>
    - Tests/MakeAnIssueTests/AppStateTests.swift (Task 1/2 additions)
    - Sources/MakeAnIssue/AppState.swift (`jobs` retention — terminal jobs are not removed on completion)
    - Sources/MakeAnIssue/IssueFilingRunner.swift (lines 111-189 — `static func file`; per-invocation UUID tempfile + `defer` cleanup at ~141-147; SC-4 verify-not-rebuild target)
    - .planning/phases/05-concurrent-filing-jobs-model/05-RESEARCH.md (Success Criterion 4 — Verified by Construction; D-06/D-07 retention tests)
    - .planning/phases/05-concurrent-filing-jobs-model/05-CONTEXT.md (D-06 retention fields, D-07 session-only)
  </read_first>
  <action>
    Add three tests plus an SC-4 source check. `testCompletedFilingJobRetainedInJobsArray`: a successful
    filing stub → `await waitUntil { state.jobs.first?.state == .done }`, assert `state.jobs.count == 1`
    and `state.jobs[0].state == .done` (the job is retained, not removed — D-06/D-07).
    `testFailedFilingJobRetainedInJobsArray`: a throwing stub (`IssueFilingError.timeout`) →
    `await waitUntil { state.jobs.first?.state == .failed }`, assert `state.jobs.count == 1` and
    `state.jobs[0].state == .failed` (failed jobs are retained for Phase 9's error rows — D-06/D-07).
    `testTwoConcurrentStubFilingsDoNotInterfere`: spawn two concurrent jobs (distinct issue numbers via
    stubs), `await waitUntil { state.jobs.filter { $0.state == .done }.count == 2 }`, assert both
    completed with their own distinct `result?.number` — observable evidence that concurrent filings do
    not share state at the jobs layer (SC-4 behavior). For the SC-4 verify-not-rebuild source check, this
    task is COMPLETE only if `IssueFilingRunner.swift` is confirmed unchanged for isolation: `file` is a
    `static func` and the tempfile uses a per-call UUID with `defer` cleanup (see acceptance grep gates) —
    do NOT modify `IssueFilingRunner.swift`. Use existing helpers; assert real behavior.
  </action>
  <verify>
    <automated>swift test --filter AppStateTests 2>&1 | tail -8</automated>
  </verify>
  <acceptance_criteria>
    - All three new tests pass under `swift test --filter AppStateTests`, and the full `swift test` suite is green.
    - A test asserts `jobs.count == 1 && jobs[0].state == .done` after success (D-06/D-07 retention) and another asserts `.failed` retention.
    - SC-4 source check: `grep -c "static func file" Sources/MakeAnIssue/IssueFilingRunner.swift` returns 1; `grep -c "make-an-issue-mcp-\\\\(UUID().uuidString)" Sources/MakeAnIssue/IssueFilingRunner.swift` returns 1; `grep -c "defer { try? FileManager.default.removeItem" Sources/MakeAnIssue/IssueFilingRunner.swift` returns 1.
    - `git diff --quiet -- Sources/MakeAnIssue/IssueFilingRunner.swift` (no changes to the isolation file).
  </acceptance_criteria>
  <done>Terminal-job retention (D-06/D-07) is locked by tests, SC-4 tempfile isolation is verified unchanged, and the full suite is green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Test seam (`onRunIssueFiling` / `onSpeak` stubs) → AppState jobs model | Tests inject deterministic filing outcomes and capture spoken text without touching the real subprocess or TTS; the boundary under test is the concurrency coordination layer, not the external `claude → docker` path. |
| N concurrent `FilingJob` Tasks ↔ shared `AppState` state | These tests are the regression guard for cross-job state bleed (distinct transcripts, distinct results). |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-5-01 | Tampering / Information Disclosure | concurrent job transcript/result handling | mitigate (test) | `testBothConcurrentJobsRetainDistinctTranscripts` and `testTwoConcurrentStubFilingsDoNotInterfere` assert no cross-job bleed — the regression guard for Plan 01's by-value capture + id-based lookup (Pitfall 1). |
| T-5-02 | Tampering | per-invocation MCP tempfile across concurrent filings (SC-4) | accept (verify-not-rebuild) | Task 3 source check asserts `IssueFilingRunner.file` stays a `static func` with UUID-named tempfile + `defer` cleanup, and that the file is unmodified — isolation by construction, not rebuilt. |
| T-5-05 | Spoofing / Denial of Service | announcement correctness | mitigate (test) | `testFailedFilingJobSpeaksGenericFailure` and the success-text test lock that speech only reflects a confirmed outcome — no false success, no missing failure. |
| T-5-SC | Tampering (supply chain) | npm/pip/cargo installs | accept (N/A) | Zero packages installed in Phase 5; no install/slopcheck checkpoint required. |
</threat_model>

<verification>
- `swift test` is green across the whole module after Task 3.
- The new test names from RESEARCH's "New Tests Required" table are present (`grep` for each in AppStateTests.swift).
- `IssueFilingRunner.swift` is unmodified (SC-4 verify-not-rebuild): `git diff --quiet -- Sources/MakeAnIssue/IssueFilingRunner.swift`.
</verification>

<success_criteria>
- CONCUR-01: a test proves capture is `.idle` with a job still `.filing` (SC-1).
- CONCUR-02: tests prove two jobs in flight at once and distinct transcripts (SC-2).
- CONCUR-03: tests prove per-job success/failure speech and defer/flush behavior (SC-3 / D-01/D-02/D-03/D-04/D-05).
- D-06/D-07: tests prove done and failed jobs are retained.
- SC-4: tempfile isolation verified unchanged in `IssueFilingRunner.swift`; concurrent stub filings complete without interference.
</success_criteria>

<output>
Create `.planning/phases/05-concurrent-filing-jobs-model/05-02-SUMMARY.md` when done.
</output>
