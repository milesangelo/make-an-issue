---
phase: 06-cancellation-stop-control
plan: 01
type: execute
wave: 0
depends_on: []
files_modified:
  - Tests/MakeAnIssueTests/CLIRunnerTests.swift
  - Tests/MakeAnIssueTests/AppStateTests.swift
autonomous: true
requirements:
  - CANCEL-01
  - CANCEL-02
  - CANCEL-03

must_haves:
  truths:
    - "A child spawned by Foundation.Process (`/bin/zsh -lc \"sleep …\"`) is its own process-group leader: getpgid(child) == child (validates Assumption A1)"
    - "The spawned child's process group differs from the test process's own group: getpgid(child) != getpgrp() (validates that kill(-pgid, …) targets the child tree, not the app — Assumption A2 safety condition)"
    - "A negative-PID signal (kill(-pid, SIGTERM)) reaps the spawned process group: kill(pid, 0) returns -1/ESRCH afterwards"
    - "The six cancel test names from RESEARCH.md Validation Architecture exist as compile-safe scaffolds and `swift test` stays green"
  artifacts:
    - path: "Tests/MakeAnIssueTests/CLIRunnerTests.swift"
      provides: "Empirical process-group gate (testSpawnedChildIsProcessGroupLeader, testNegativePIDSignalReapsProcessGroup) + cancel scaffolds (testCancelKillsProcessGroup, testCancelAndExitBoundaryResolvesExactlyOnce)"
      contains: "testSpawnedChildIsProcessGroupLeader"
    - path: "Tests/MakeAnIssueTests/AppStateTests.swift"
      provides: "Cancel scaffolds (testCancelJobIdTransitionsToCancel, testCancelAnnouncementDeferredDuringRecording, testCancelAllCancelsEveryInFlightJob, testCancelledJobRetainedInJobsList)"
      contains: "testCancelJobIdTransitionsToCancel"
  key_links:
    - from: "Tests/MakeAnIssueTests/CLIRunnerTests.swift (empirical gate)"
      to: "Sources/MakeAnIssue/CLIRunner.swift (the kill(-pgid) approach implemented in 06-02)"
      via: "getpgid(child) == child && getpgid(child) != getpgrp() proves negative-PID group kill is safe before any production kill relies on it"
      pattern: "getpgid"
  prohibitions:
    - "If getpgid(child) equals getpgrp() (the child inherited the APP's process group), the kill(-pgid, …) approach is INVALID — it would signal the app itself. HALT the phase and replan a per-process-tree-walk fallback; do NOT proceed to 06-02."
    - "Never write a scaffold body that references a not-yet-created symbol (cancel(jobID:), cancelAll(), forceKillAllProcessTrees(), processGroupID) — the whole test target would fail to compile. Scaffolds throw XCTSkip only."
---

<objective>
Establish the Wave-0 verification floor for Phase 6 before any cancellation code is written:

1. Empirically validate Assumptions A1/A2 from RESEARCH.md — that Foundation.Process places each
   spawned child in its own process group (getpgid(child) == child) distinct from the app's group
   (getpgid(child) != getpgrp()), and that a negative-PID signal reaps that group. This GATES the
   entire `kill(-pgid, …)` mechanism the rest of the phase depends on; if it fails, the approach is
   invalid and the phase must be replanned.
2. Create compile-safe XCTest scaffolds (RED via `XCTSkip`) for the six cancel behaviors named in
   RESEARCH.md Validation Architecture, so the implementation waves flesh them out rather than
   inventing test names, and the test contract is committed first (Nyquist).

Purpose: De-risk the load-bearing process-group assumption and lock the test plan before
implementation (CANCEL-01/02/03).
Output: Two new tests proving the group-kill mechanics, six skipped scaffolds across the two test
files, and a green `swift test`.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/06-cancellation-stop-control/06-CONTEXT.md
@.planning/phases/06-cancellation-stop-control/06-RESEARCH.md
@.planning/phases/06-cancellation-stop-control/06-PATTERNS.md
@.claude/skills/spike-findings-make-an-issue/SKILL.md
</context>

## Artifacts this phase produces

New symbols introduced by THIS plan (test scaffolds + empirical gate — the source-grounding pass
should treat these as newly created, not drift):

- **Changed** `Tests/MakeAnIssueTests/CLIRunnerTests.swift`:
  - `func testSpawnedChildIsProcessGroupLeader() async throws` — real, passes; validates A1/A2.
  - `func testNegativePIDSignalReapsProcessGroup() async throws` — real, passes; validates the negative-PID group reap.
  - `func testCancelKillsProcessGroup() async throws` — scaffold (`XCTSkip`), fleshed out in 06-02.
  - `func testCancelAndExitBoundaryResolvesExactlyOnce() async throws` — scaffold (`XCTSkip`), fleshed out in 06-02.
- **Changed** `Tests/MakeAnIssueTests/AppStateTests.swift`:
  - `func testCancelJobIdTransitionsToCancel() async throws` — scaffold (`XCTSkip`), fleshed out in 06-03.
  - `func testCancelAnnouncementDeferredDuringRecording() async throws` — scaffold (`XCTSkip`), fleshed out in 06-03.
  - `func testCancelAllCancelsEveryInFlightJob() async throws` — scaffold (`XCTSkip`), fleshed out in 06-03.
  - `func testCancelledJobRetainedInJobsList() async throws` — scaffold (`XCTSkip`), fleshed out in 06-03.

Downstream symbols this phase will create in later plans (named here so reviewers exclude them from
drift): `AppState.cancel(jobID:)`, `AppState.cancelAll()`, `AppState.forceKillAllProcessTrees()`,
`FilingJob.processGroupID: pid_t?`, `IssueFilingRunner.file(onProcessStarted:)`,
`CLIRunner.run(onSpawn:)`, `AppDelegate.applicationShouldTerminate(_:)`, `AppDelegate.sweepMCPTempFiles()`.

<tasks>

<task type="auto">
  <name>Task 1: Empirical process-group gate — validate A1/A2 before any kill(-pgid) code</name>
  <files>Tests/MakeAnIssueTests/CLIRunnerTests.swift</files>
  <read_first>
    - Tests/MakeAnIssueTests/CLIRunnerTests.swift (whole file — the timeout tests at lines 48-85 are the closest analog for spawning a real subprocess and the `import` block at line 1; new tests append before the final closing brace at line 147)
    - Sources/MakeAnIssue/CLIRunner.swift (lines 82-92 — how `Process` is configured: `/bin/zsh`, arguments `["-lc", command]` — the exact spawn shape to mirror so the gate measures the real production process)
    - .planning/phases/06-cancellation-stop-control/06-RESEARCH.md (Assumptions Log A1/A2; Open Questions 1; Discretion Item 1 — "macOS Process Group Behavior")
    - .planning/phases/06-cancellation-stop-control/06-PATTERNS.md (Process-Group Kill shared pattern)
  </read_first>
  <behavior>
    - Spawning `/bin/zsh -lc "sleep 30"` via Foundation.Process yields a child whose process group id equals its own pid: getpgid(child.processIdentifier) == child.processIdentifier (A1).
    - That child's process group differs from the running test process's own group: getpgid(child.processIdentifier) != getpgrp() (A2 safety — kill(-pgid, …) will not signal the app).
    - Sending kill(-child.processIdentifier, SIGTERM) reaps the group: within a short bounded poll, kill(child.processIdentifier, 0) returns -1 with errno ESRCH.
    - Both tests clean up any surviving child (kill(-pid, SIGKILL)) so they leave no orphan, even on assertion failure.
  </behavior>
  <action>
    Add `testSpawnedChildIsProcessGroupLeader` to CLIRunnerTests. Configure a `Process` exactly as
    CLIRunner does — `executableURL` = `/bin/zsh`, `arguments` = `["-lc", "sleep 30"]` — call
    `try process.run()`, then read the kernel-reported group with `getpgid(process.processIdentifier)`
    (import Darwin is already transitively available via Foundation). Assert it equals
    `process.processIdentifier` (A1 — the child is its own group leader) and assert it does NOT equal
    `getpgrp()` (A2 — the child is not in the app/test process group, so a group-directed signal stays
    inside the child tree). In a `defer`, force-reap with `kill(-process.processIdentifier, SIGKILL)` so
    the 30-second sleep never lingers. Add an explanatory comment that a failure of either assertion means
    the negated-identifier group-kill approach assumed by 06-02/03/04 is unsafe and the phase must halt.

    Add `testNegativePIDSignalReapsProcessGroup`. Spawn the same `/bin/zsh -lc "sleep 30"` Process, then
    send a group-directed terminate with `kill(-process.processIdentifier, SIGTERM)`. Poll (reuse the
    `ContinuousClock` deadline style from the existing timeout tests, or a short bounded loop with
    `Task.sleep(for: .milliseconds(20))`) until `kill(process.processIdentifier, 0) == -1` and the global
    `errno == ESRCH` (no such process — the group leader was reaped), with a generous bound (e.g. up to
    3 seconds) so CI load does not flake. Assert the group was reaped within the bound. Add a `defer`
    SIGKILL group cleanup as a safety net. Use only POSIX (`kill`, `getpgid`, `getpgrp`, `errno`,
    `ESRCH`) and Foundation.Process — no new dependency, no `claude`/Docker required.
  </action>
  <verify>
    <automated>swift test --filter CLIRunnerTests.testSpawnedChildIsProcessGroupLeader 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - `swift test --filter CLIRunnerTests.testSpawnedChildIsProcessGroupLeader` exits 0.
    - `swift test --filter CLIRunnerTests.testNegativePIDSignalReapsProcessGroup` exits 0.
    - `grep -c "getpgid" Tests/MakeAnIssueTests/CLIRunnerTests.swift` returns at least 2 (the A1 leader check and the A2 app-group check).
    - Source assertion: `testSpawnedChildIsProcessGroupLeader` asserts BOTH `getpgid(pid) == pid` AND `getpgid(pid) != getpgrp()`.
    - Source assertion: `testNegativePIDSignalReapsProcessGroup` sends a negated-identifier signal and asserts the leader is gone via `kill(pid, 0)` → ESRCH.
    - GATE: if either test fails, do not start 06-02 — the kill(-pgid) mechanism is invalid; halt and replan.
  </acceptance_criteria>
  <done>The two empirical tests pass, confirming A1 (child is its own group leader) and A2 (group is distinct from the app) and that a negative-PID signal reaps the group — the kill(-pgid) approach is validated as safe to build on.</done>
</task>

<task type="auto">
  <name>Task 2: Compile-safe cancel test scaffolds (RED via XCTSkip) in both test files</name>
  <files>Tests/MakeAnIssueTests/CLIRunnerTests.swift, Tests/MakeAnIssueTests/AppStateTests.swift</files>
  <read_first>
    - Tests/MakeAnIssueTests/CLIRunnerTests.swift (the `XCTestCase` shape, lines 1-6; append new scaffolds before the closing brace at line 147)
    - Tests/MakeAnIssueTests/AppStateTests.swift (the `@MainActor final class AppStateTests` shape, lines 1-17; the `waitUntil` helper 681-692 and `makeRepo` 694-698 the impl waves will use; append scaffolds before the final closing brace at line 1182)
    - .planning/phases/06-cancellation-stop-control/06-RESEARCH.md (Validation Architecture — Phase Requirements → Test Map table; Wave 0 Gaps checklist)
    - .planning/phases/06-cancellation-stop-control/06-PATTERNS.md (CLIRunnerTests / AppStateTests "What to add" sections — the exact test names and intents)
  </read_first>
  <behavior>
    - All six cancel test methods exist with the exact names from RESEARCH.md so the impl waves only replace bodies.
    - Each scaffold body throws `XCTSkip` with a note pointing to its implementing plan (06-02 or 06-03); it references no symbol that does not yet exist, so the test target compiles.
    - `swift test` is green (skipped tests are not failures).
  </behavior>
  <action>
    In CLIRunnerTests, add two scaffold methods whose entire body is a single `throw XCTSkip("Pending —
    fleshed out in 06-02")`: `testCancelKillsProcessGroup()` and
    `testCancelAndExitBoundaryResolvesExactlyOnce()` (both `async throws`). Add a brief comment above each
    naming what 06-02 will assert (respectively: cancelling the enclosing Task reaps the subprocess group
    within the grace window; the cancel-vs-exit race resolves the continuation exactly once).

    In AppStateTests, add four scaffold methods whose entire body is a single `throw XCTSkip("Pending —
    fleshed out in 06-03")` (all `async throws`, inside the `@MainActor` class):
    `testCancelJobIdTransitionsToCancel()`, `testCancelAnnouncementDeferredDuringRecording()`,
    `testCancelAllCancelsEveryInFlightJob()`, `testCancelledJobRetainedInJobsList()`. Add a brief comment
    above each naming the 06-03 contract in prose (respectively: cancelling one job drives it to the
    cancelled state and speaks the cancelled phrase; the cancelled announcement is deferred while
    recording and flushed after; cancelling all cancels every in-flight job; a cancelled job is retained
    in the jobs list, not deleted — D-02/D-03). CRITICAL: the scaffold bodies must reference ONLY the
    `XCTSkip` call — they must not name any AppState cancel method or job field that the later plans
    introduce, because those symbols do not exist yet and any reference would break the test target's
    compilation. Do not modify any production source file in this plan.
  </action>
  <verify>
    <automated>swift test 2>&1 | tail -8</automated>
  </verify>
  <acceptance_criteria>
    - `swift test` compiles the full module and is green (the four new scaffolds report as skipped, the two Task-1 tests pass).
    - `grep -c "func testCancel" Tests/MakeAnIssueTests/CLIRunnerTests.swift` returns at least 2.
    - `grep -c "func testCancel" Tests/MakeAnIssueTests/AppStateTests.swift` returns at least 3 (testCancelJobId…, testCancelAnnouncement…, testCancelAll…) plus testCancelledJobRetained… → 4 total.
    - `grep -c "XCTSkip" Tests/MakeAnIssueTests/CLIRunnerTests.swift` returns at least 2 and `grep -c "XCTSkip" Tests/MakeAnIssueTests/AppStateTests.swift` returns at least 4.
    - The scaffolds reference no uncreated symbol — proven by `swift test` compiling cleanly (a reference to a not-yet-created AppState cancel method or job field would be a compile error, not a skip).
  </acceptance_criteria>
  <done>All six cancel test names exist as compile-safe `XCTSkip` scaffolds across the two files; the suite is green and the implementation waves have a committed test contract to flesh out.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Test process → spawned `/bin/zsh -lc "sleep …"` subprocess | The gate intentionally crosses into a real OS process group to measure pgid relationships and signal delivery; no untrusted input, but a leaked sleep would be an orphan. |
| Negated-identifier signal (`kill(-pid, …)`) → process group | The whole phase rests on this signal reaching only the child tree; the gate proves the child group is distinct from the app group before any production code relies on it. |

## STRIDE Threat Register

(Block-on: high · ASVS L1. The three Phase-6 signal-handling threats are established here and mitigated in the implementation waves.)

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-6-01 | Tampering | Signal sent to wrong process group (pgid=0 or the app's own group) | mitigate (validated here) | `testSpawnedChildIsProcessGroupLeader` asserts getpgid(child) != getpgrp(), empirically proving a group-directed signal stays in the child tree and never hits the app's group. Production guard `if capturedPGID > 0` is added in 06-02/06-03. |
| T-6-02 | Elevation of privilege (persistent `--rm` container) | SIGKILL-before-SIGTERM ordering | mitigate (scaffolded here) | `testCancelKillsProcessGroup` scaffold reserved for 06-02, where SIGTERM-first → bounded grace → SIGKILL is implemented and asserted. |
| T-6-03 | Denial of service | `applicationShouldTerminate` reply skipped → app hangs | mitigate (scaffolded here) | Quit-path reply guarantee (`defer { NSApp.reply(toApplicationShouldTerminate: true) }`) lands in 06-04; no quit code in this plan. |
| T-6-SC | Tampering (supply chain) | npm/pip/cargo installs | accept (N/A) | Phase 6 installs zero packages (RESEARCH Package Legitimacy Audit: not applicable). No dependency supply-chain surface; no install/slopcheck checkpoint required. |
</threat_model>

<verification>
- `swift test --filter CLIRunnerTests.testSpawnedChildIsProcessGroupLeader` and `…testNegativePIDSignalReapsProcessGroup` both exit 0 (A1/A2 validated).
- `swift test` compiles and is green with the four AppStateTests scaffolds skipped.
- No production source file under `Sources/` is modified by this plan (`git diff --name-only` lists only the two test files).
- GATE: a failure of either empirical test halts the phase (the kill(-pgid) mechanism is invalid).
</verification>

<success_criteria>
- Assumptions A1 (getpgid(child) == child) and A2 (getpgid(child) != getpgrp()) are empirically confirmed, and a negative-PID signal reaps the group — the kill(-pgid) approach is safe to build on (CANCEL-01 foundation).
- The six cancel behaviors from RESEARCH.md Validation Architecture exist as named, compile-safe scaffolds (CANCEL-01/02/03 test contract committed).
- `swift test` is green; the phase has a verified floor before implementation begins.
</success_criteria>

<output>
Create `.planning/phases/06-cancellation-stop-control/06-01-SUMMARY.md` when done.
</output>
