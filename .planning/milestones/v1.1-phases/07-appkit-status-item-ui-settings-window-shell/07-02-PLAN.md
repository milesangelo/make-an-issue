---
phase: 07-appkit-status-item-ui-settings-window-shell
plan: 02
type: execute
wave: 2
depends_on:
  - 07-01
files_modified:
  - Sources/MakeAnIssue/MenuView.swift
autonomous: true
requirements:
  - SETTINGS-01
user_setup: []

must_haves:
  truths:
    - "The push-to-talk shortcut editor no longer appears in the popover; the popover Settings disclosure contains only the CLI Command field (D-04/D-05)"
    - "The read-only ShortcutPillView still shows the current shortcut in ActionCard (D-05)"
    - "After opening then closing the popover with another app focused, the global push-to-talk shortcut still fires (Pitfall 10 / A4)"
    - "After opening then closing the right-click menu with another app focused, the global push-to-talk shortcut still fires"
  artifacts:
    - path: "Sources/MakeAnIssue/MenuView.swift"
      provides: "Popover content with the shortcut recorder editor removed and the manual menu end-tracking workaround removed; CLI Command field and ShortcutPillView retained"
      contains: "cliCommand"
  key_links:
    - from: "Sources/MakeAnIssue/MenuView.swift"
      to: "Sources/MakeAnIssue/SettingsView.swift"
      via: "the push-to-talk recorder editor now lives only in SettingsView (relocated, not duplicated) per D-03"
      pattern: "ShortcutPillView"
---

<objective>
Complete the Recorder relocation (D-03) and the Pitfall 10 hotkey fix in the popover content.
Remove two blocks from `MenuView`: (1) the push-to-talk shortcut editor inside the Settings
disclosure (lines 63–71) — now homed in `SettingsView` (Plan 01) per D-03; (2) the `.onDisappear`
modifier (lines 104–113) that manually posts a menu end-tracking notification — that workaround was
specific to `MenuBarExtra`'s unbalanced tracking and would spuriously un-pause the global hotkey
under the new `NSPopover`/`NSMenu` shell (RESEARCH Pattern 7 / Pitfall 10 / Assumption A4).

Purpose: finish SETTINGS-01's Settings-window story (the focus-sensitive editor lives in the real
window, not the transient popover) and protect the global push-to-talk shortcut's survival across
popover/menu open-close cycles (ROADMAP success criterion 4).

Output: `MenuView.swift` with both blocks removed and everything else — CLI Command field (D-04),
ShortcutPillView read-only display (D-05), and all subviews — preserved.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/07-appkit-status-item-ui-settings-window-shell/07-CONTEXT.md
@.planning/phases/07-appkit-status-item-ui-settings-window-shell/07-RESEARCH.md
@.planning/phases/07-appkit-status-item-ui-settings-window-shell/07-PATTERNS.md
@.planning/phases/07-appkit-status-item-ui-settings-window-shell/07-VALIDATION.md
@Sources/MakeAnIssue/MenuView.swift
@Sources/MakeAnIssue/SettingsView.swift
</context>

## Artifacts this phase produces (Plan 02)

| Symbol / file | Kind | Notes |
|---------------|------|-------|
| `Sources/MakeAnIssue/MenuView.swift` (Settings disclosure) | MODIFIED — removal | Push-to-talk shortcut editor block (lines 63–71) removed; relocated to `SettingsView` (Plan 01) per D-03 |
| `Sources/MakeAnIssue/MenuView.swift` (`.onDisappear`) | MODIFIED — removal | Manual menu end-tracking notification post (lines 104–113) removed per Pitfall 10 / RESEARCH Pattern 7 |
| Retained: CLI Command `TextField` (lines 73–80), `ShortcutPillView` in `ActionCard` (line 228) | PRESERVED | D-04 keeps the CLI field this phase; D-05 keeps the read-only shortcut display |

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Remove the popover shortcut editor and the menu end-tracking workaround from MenuView (D-03/D-05, Pitfall 10)</name>
  <read_first>
    - `Sources/MakeAnIssue/MenuView.swift` in full (lines 1–128) — locate the disclosure `VStack` shortcut-editor block (lines 63–71), the CLI Command `TextField` block to KEEP (lines 73–80), and the `.onDisappear` modifier to remove (lines 104–113); confirm `updateShortcutText()` (lines 116–122) and the `.onAppear`/`.onReceive` modifiers (lines 98–103) stay
    - 07-PATTERNS.md § "Sources/MakeAnIssue/MenuView.swift (MODIFIED — two removals only)" — the exact KEEP list and both REMOVE blocks
    - 07-RESEARCH.md § "Pattern 7: Remove MenuView.onDisappear Workaround" and § "Pitfall D" (why carrying the manual post over breaks hotkey balance under NSPopover/NSMenu)
    - `Sources/MakeAnIssue/SettingsView.swift` (Plan 01) — confirm the shortcut editor now lives there before deleting it here (relocate, do not orphan)
  </read_first>
  <files>Sources/MakeAnIssue/MenuView.swift</files>
  <action>
    Delete the push-to-talk shortcut editor block inside the Settings disclosure `VStack`
    (lines 63–71): the secondary `Text("Push-to-Talk Shortcut")` label, the shortcut-editor view,
    and the trailing `.padding(.top, 4)` on that inner `VStack`. This editor is now homed in
    `SettingsView` per D-03 (Plan 01) — this is a relocation, so verify `SettingsView.swift` exists
    first. Leave the CLI Command `TextField` block (lines 73–80) intact inside the disclosure per
    D-04, so the disclosure retains only the CLI Command field this phase.

    Delete the `.onDisappear { … }` modifier (lines 104–113) that manually posts the menu
    end-tracking notification to rebalance the global hotkey. Per RESEARCH Pattern 7 / Pitfall 10:
    `NSPopover` never enters menu-tracking mode and a real `NSMenu` (Plan 01) posts balanced
    begin/end notifications, so the manual post would spuriously un-pause push-to-talk under the new
    shell. Remove the whole modifier, not just its body.

    KEEP everything else verbatim: the three imports (including `import KeyboardShortcuts`, still
    needed by `updateShortcutText()` / `getShortcut(for: .pushToTalk)`), `@EnvironmentObject appState`,
    `@AppStorage(AppState.cliCommandKey) cliCommand`, the header/RepositoryCard/ActionCard/
    StatusBanner/TranscriptCard, the disclosure label, the `.onAppear`/`.onReceive` modifiers,
    `updateShortcutText()`, `shouldShowStatusBanner`, and every subview struct including
    `ShortcutPillView` referenced from `ActionCard` (line 228) per D-05. Do not relocate the CLI
    field (that is Phase 8 / FINDING-06) and do not touch the read-only shortcut pill display.
  </action>
  <verify>
    <automated>! grep -nE 'KeyboardShortcuts\.Recorder' Sources/MakeAnIssue/MenuView.swift && ! grep -nE 'didEndTrackingNotification' Sources/MakeAnIssue/MenuView.swift && grep -q 'cliCommand' Sources/MakeAnIssue/MenuView.swift && grep -q 'ShortcutPillView' Sources/MakeAnIssue/MenuView.swift && grep -q 'import KeyboardShortcuts' Sources/MakeAnIssue/MenuView.swift && grep -q 'KeyboardShortcuts.Recorder' Sources/MakeAnIssue/SettingsView.swift && swift build 2>&1 | tail -5 && swift test 2>&1 | tail -10</automated>
  </verify>
  <acceptance_criteria>
    - `MenuView.swift` no longer contains the shortcut-editor view (`! grep -nE 'KeyboardShortcuts\.Recorder'` passes) — it now lives only in `SettingsView.swift` (cross-file positive check passes).
    - `MenuView.swift` no longer contains the manual menu end-tracking post (`! grep -nE 'didEndTrackingNotification'` passes); the whole `.onDisappear` modifier is gone.
    - The CLI Command field (`cliCommand`), the `ShortcutPillView` display, and `import KeyboardShortcuts` are retained.
    - `swift build` succeeds AND `swift test 2>&1` is green.
    - Behavioral proof (popover shows only the CLI field in the disclosure; ShortcutPillView still shows the shortcut; push-to-talk survives popover/menu open-close with another app focused) → 07-VALIDATION.md Manual-Only Verifications / 07-RESEARCH.md macOS 13/14/15 UAT checklist.
  </acceptance_criteria>
  <done>The popover hosts only the CLI Command field in its disclosure, the read-only shortcut pill remains, the menu end-tracking workaround is gone, and the build + model suite stay green. Hotkey survival is confirmed by manual UAT.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| user ↔ menu-bar popover UI | Local single-user interaction only. This plan only REMOVES UI/code from the popover content; it adds no input surface. |

This plan introduces NO new trust boundary versus Plan 01: no new input surfaces, data stores,
network, or subprocess calls, and no external packages. Removing the manual menu end-tracking post
is a correctness change to keep the global push-to-talk shortcut firing (a functional/availability
property verified by UAT, not a security control).

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-07-03 | (none introduced) | `MenuView` popover content (removals only) | accept | No STRIDE-applicable threat is added: the change deletes a shortcut editor and a notification post. Push-to-talk availability across popover/menu cycles is a functional property proven by the 07-VALIDATION.md manual UAT (Pitfall 10 / A4), not a security mitigation. The Quit→teardown (T-07-01) and focus behavior (T-07-02) are owned and unchanged by Plan 01. |
</threat_model>

<verification>
**Automated:** `swift build` then `swift test 2>&1` green (model-layer regression guard — the
popover content is not unit-testable; see 07-VALIDATION.md).

**Source assertions:** the shortcut editor and the manual menu end-tracking post are absent from
`MenuView.swift`; the editor is present in `SettingsView.swift`; the CLI field, ShortcutPillView,
and `import KeyboardShortcuts` are retained.

<human-check>
Behavioral proof is MANUAL-ONLY UAT on macOS 13/14/15 — see 07-VALIDATION.md § Manual-Only
Verifications and 07-RESEARCH.md § macOS Version UAT Checklist: popover Settings disclosure shows
only the CLI Command field; ShortcutPillView still displays the current shortcut read-only; with
Finder focused, push-to-talk fires after opening then closing the popover AND after opening then
closing the right-click menu, and fires while the popover is open. This closes Assumption A4.
</human-check>
</verification>

<success_criteria>
- `swift build` and `swift test 2>&1` are green.
- The push-to-talk shortcut editor is removed from the popover and present only in `SettingsView`;
  the manual menu end-tracking workaround is removed; the CLI field (D-04) and ShortcutPillView
  (D-05) are retained.
- macOS 13/14/15 manual UAT confirms hotkey survival across popover/menu open-close cycles
  (ROADMAP success criterion 4) before `/gsd-verify-work`.
</success_criteria>

<output>
Create `.planning/phases/07-appkit-status-item-ui-settings-window-shell/07-02-SUMMARY.md` when done.
Record the empirical push-to-talk survival result (popover cycles, menu cycles, popover-open) and
the macOS versions UAT'd — this is the A4 closure evidence.
</output>
