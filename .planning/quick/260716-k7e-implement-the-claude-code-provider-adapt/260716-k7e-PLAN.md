---
quick_id: 260716-k7e
status: ready
mode: quick-full
date: 2026-07-16
must_haves:
  truths:
    - "Claude Code runs from typed executable plus literal argv in the prepared workspace with a bounded prompt containing trusted instructions and quoted issue context."
    - "Provider children receive only the adapter allowlist, terminate as a process group on timeout or cancellation, and return a typed bounded outcome."
    - "Provider artifacts are redacted before persistence and successful execution advances the ledger from running to validating."
  artifacts:
    - "Sources/MakeAnIssueWorkerCore/ProviderAdapter.swift"
    - "Sources/MakeAnIssueWorkerCore/ProcessExecutor.swift"
    - "Sources/MakeAnIssueWorkerCore/RunService.swift"
    - "Tests/MakeAnIssueWorkerTests/ProviderAdapterTests.swift"
  key_links:
    - "WorkerRunPipeline resolves AgentConfig.provider and invokes ClaudeCodeProviderAdapter with PreparedWorkspace."
    - "ClaudeCodeProviderAdapter delegates every child process to ProcessExecuting and persists output through ArtifactStore."
---

# Quick Task 260716-k7e: Claude Code worker provider adapter

## Task 1: Typed provider execution and lifecycle outcomes

**Files:** `ProviderAdapter.swift`, `ProcessExecutor.swift`

**Action:** Add the provider protocol/seam and Claude Code implementation. Build a bounded prompt file from snapshotted agent instructions plus JSON-quoted issue title/body/labels; append adapter-owned edit-only and structured-output CLI arguments; use an isolated HOME/temp/PWD and explicit environment allowlist; recheck executable identity. Extend the shared executor with stdin-file transport, cancellation polling, duration, cancellation, and stderr truncation metadata while preserving process-group TERM-to-KILL behavior.

**Verify:** Deterministic fixture scripts cover success, nonzero exit, timeout/process-tree kill, cancellation, configured argv, prompt content, and environment sanitization.

**Done:** Outcomes distinguish `completed`, `failed`, `timed-out`, and `cancelled`, with exit code, duration, bounded stdout/stderr, and no second executor.

## Task 2: Redacted artifacts and durable ledger metadata

**Files:** `DiffInspector.swift`, `RunLedger.swift`, `ProviderAdapter.swift`

**Action:** Add token-shaped-string redaction before provider output is written, cap the final persisted provider log, and record a compact JSON adapter outcome in the run ledger without persisting raw output.

**Verify:** A fixture prints GitHub/Anthropic-style secrets; persisted provider logs contain redaction markers and no original token. Ledger tests assert outcome status, exit, and duration metadata.

**Done:** Raw provider output never reaches run artifacts or ledger details.

## Task 3: RunService integration and end-to-end fixture execution

**Files:** `RunService.swift`, `WorkerArtifactSmokeTests.swift`, `ProviderAdapterTests.swift`, test support as needed

**Action:** Carry issue body through inspection, replace the environment-selected fixture hook with provider selection from the immutable typed config, map adapter terminal outcomes to distinct retained failure codes, and advance `running -> validating` only for completed provider execution.

**Verify:** RunService drives a configured fixture Claude executable against a real prepared workspace; success reaches validation/publication, nonzero and timeout retain work, and the full `swift test` suite plus `git diff --check` pass.

**Done:** The worker executes the configured Claude Code adapter for real with unchanged publication invariants.
